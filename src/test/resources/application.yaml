spring:
    application:
        name: ai
    cache:
        type: caffeine
        expire-after-write-minutes: 10
        maximum-size: 100
    config:
        import: ""
    cloud:
        vault:
            enabled: false

openai:
    key: test-key
    connect-timeout-ms: 2000
    response-timeout-seconds: 8
    retry:
        max-attempts: 3
        initial-delay-ms: 1000
        max-delay-ms: 8000
